# Python tooling for the corridor_docs package.
# The documentation site itself is built with Astro Starlight - see package.json.
# Runtime deps (click, flask, requests) are declared in pyproject.toml.

ruff  # Linter + formatter for python files
pyright  # Typing checks for python files
build  # To build python packages
